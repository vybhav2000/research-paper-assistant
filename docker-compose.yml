services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: research-paper-assistant:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
